{
  "name": "superbowl-squares-subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ superbowl-squares",
    "create-local": "graph create --node http://localhost:8020/ superbowl-squares",
    "remove-local": "graph remove --node http://localhost:8020/ superbowl-squares",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 superbowl-squares"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.67.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
